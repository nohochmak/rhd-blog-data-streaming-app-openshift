(function(window) {
    window["env"] = window["env"] || {};
    window["env"]["topNSize"] = "${TOP_N_SIZE}";
    window["env"]["queueSize"] = "${QUEUE_SIZE}";
    window["env"]["apiHost"] = "${API_HOST}"
    window["env"]["apiPort"] = "${API_PORT}"
  })(this);
