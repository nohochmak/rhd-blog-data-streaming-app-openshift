import { SliceCollectionPipe } from './slice-collection.pipe';

describe('SliceCollectionPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceCollectionPipe();
    expect(pipe).toBeTruthy();
  });
});
