import { SortEmojiPipe } from './sort-emoji.pipe';

describe('SortEmojiPipe', () => {
  it('create an instance', () => {
    const pipe = new SortEmojiPipe();
    expect(pipe).toBeTruthy();
  });
});
